# Project root (used by some scripts)
PROJECT_HOME=~/path/to/visreps

# === Dataset Paths ===
# ImageNet (required for training)
IMAGENET_DATA_DIR=/path/to/imagenet/train
IMAGENET_LOCAL_DIR=/path/to/imagenet           # Must contain folder_labels.json

# Tiny ImageNet (optional, for smaller-scale experiments)
TINY_IMAGENET_DATA_DIR=/path/to/tiny-imagenet
TINY_IMAGENET_LOCAL_DIR=/path/to/tiny-imagenet

# ImageNet-Mini subsets (optional)
IMAGENET_MINI_50_DATA_DIR=/path/to/imagenet-mini-50

# === Neural Dataset Paths (required for evaluation) ===
# Natural Scenes Dataset (fMRI)
NSD_DATA_DIR=datasets/neural/nsd
NSD_SYNTHETIC_DATA_DIR=datasets/neural/nsd_synthetic

# TVSD (macaque electrophysiology) — uses THINGS images via BONNER_DATASETS_HOME
TVSD_DATA_DIR=/path/to/tvsd
BONNER_DATASETS_HOME=~/.cache/bonner-datasets

# === Optional Services ===
# Weights & Biases (for experiment tracking)
# WANDB_API_KEY=your_key_here

# uv virtual environment location
UV_PROJECT_ENVIRONMENT=".venv"
